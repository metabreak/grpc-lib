{
  "name": "@metabreak/grpc-worker",
  "description": "Angular gRPC framework: worker package",
  "author": "smnbbrv",
  "license": "MIT",
  "main": "./lib/index.js",
  "scripts": {
    "clean": "yarn rimraf ./lib",
    "build": "yarn clean && yarn tsc -p ./tsconfig.json"
  },
  "dependencies": {
    "@metabreak/grpc-worker-common": "workspace:*",
    "grpc-web": "1.2.1"
  },
  "devDependencies": {
    "rimraf": "3.0.2",
    "typescript": "4.3.5"
  }
}
